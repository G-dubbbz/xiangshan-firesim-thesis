diff --git a/.github/scripts/install-conda.sh b/.github/scripts/install-conda.sh
index b6fcce18..f0f1f642 100755
--- a/.github/scripts/install-conda.sh
+++ b/.github/scripts/install-conda.sh
@@ -166,7 +166,7 @@ else
     $SUDO "$CONDA_EXE" install $DRY_RUN_OPTION -y -n base conda-libmamba-solver
 
     # Use the fast solver by default
-    "${DRY_RUN_ECHO[@]}" $SUDO "$CONDA_EXE" config --system --set experimental_solver libmamba
+    "${DRY_RUN_ECHO[@]}" $SUDO "$CONDA_EXE" config --system --set solver libmamba
 
     # conda-lock is a special case and must always be installed into the base environment
     $SUDO "$CONDA_EXE" install $DRY_RUN_OPTION -y -n base conda-lock=1.4
diff --git a/conda-reqs/chipyard-base.yaml b/conda-reqs/chipyard-base.yaml
index 4cfcbdc3..a258be7b 100644
--- a/conda-reqs/chipyard-base.yaml
+++ b/conda-reqs/chipyard-base.yaml
@@ -16,7 +16,7 @@ dependencies:
     #   documentation on package_spec syntax for constraining versions
     - gcc=13.2
     - gxx=13.2
-    - sysroot_linux-64=2.34 # need to be close to system glibc for VCS compatibility
+    - sysroot_linux-64=2.35
     - conda-gcc-specs
     - binutils
 
diff --git a/scripts/build-setup.sh b/scripts/build-setup.sh
index 709fc4db..1372cc94 100755
--- a/scripts/build-setup.sh
+++ b/scripts/build-setup.sh
@@ -340,7 +340,7 @@ if run_step "10"; then
     else
 	echo "Downloading CIRCT from nightly build"
 
-	git submodule update --init $CYDIR/tools/install-circt &&
+	git submodule update --init --filter=blob:none $CYDIR/tools/install-circt &&
 	    $CYDIR/tools/install-circt/bin/download-release-or-nightly-circt.sh \
 		-f circt-full-static-linux-x64.tar.gz \
 		-i $PREFIX \
diff --git a/scripts/init-submodules-no-riscv-tools-nolog.sh b/scripts/init-submodules-no-riscv-tools-nolog.sh
index c05fccbb..c5437069 100755
--- a/scripts/init-submodules-no-riscv-tools-nolog.sh
+++ b/scripts/init-submodules-no-riscv-tools-nolog.sh
@@ -216,14 +216,14 @@ cd "$RDIR"
     (
         set -x
         git_submodule_exclude _skip
-        git submodule update --init --recursive || exit 1
+        git submodule update --init --filter=blob:none --recursive || exit 1
     )
 )
 
 (
     if [[ "$ENABLE_CVA6" -eq 1 ]] ; then
         submodule_name="generators/cva6"
-        git submodule update --init generators/cva6 || exit 1
+        git submodule update --init --filter=blob:none generators/cva6 || exit 1
         git -C generators/cva6 submodule update --init src/main/resources/cva6/vsrc/cva6 || exit 1
         git -C generators/cva6/src/main/resources/cva6/vsrc/cva6 submodule update --init src/axi || exit 1
         git -C generators/cva6/src/main/resources/cva6/vsrc/cva6 submodule update --init src/axi_riscv_atomics || exit 1
@@ -236,84 +236,84 @@ cd "$RDIR"
 
     if [[ "$ENABLE_NVDLA" -eq 1 ]] ; then
         submodule_name="generators/nvdla"
-        git submodule update --init generators/nvdla || exit 1
+        git submodule update --init --filter=blob:none generators/nvdla || exit 1
         git -C generators/nvdla submodule update --init src/main/resources/hw || exit 1
     fi
 
     if [[ "$ENABLE_ARA" -eq 1 ]] ; then
-	git submodule update --init generators/ara || exit 1
+	git submodule update --init --filter=blob:none generators/ara || exit 1
 	git -C generators/ara submodule update --init ara || exit 1
     fi
 
     if [[ "$ENABLE_CALIPTRA" -eq 1 ]] ; then
-	git submodule update --init generators/caliptra-aes-acc || exit 1
+	git submodule update --init --filter=blob:none generators/caliptra-aes-acc || exit 1
     fi
 
     if [[ "$ENABLE_FFT" -eq 1 ]] ; then
-	git submodule update --init generators/fft-generator || exit 1
+	git submodule update --init --filter=blob:none generators/fft-generator || exit 1
     fi
 
     if [[ "$ENABLE_RADIANCE" -eq 1 ]] ; then
-	git submodule update --init --recursive generators/radiance || exit 1
+	git submodule update --init --filter=blob:none --recursive generators/radiance || exit 1
     fi
 
     if [[ "$ENABLE_COMPRESSACC" -eq 1 ]] ; then
-	git submodule update --init generators/compress-acc || exit 1
+	git submodule update --init --filter=blob:none generators/compress-acc || exit 1
     fi
 
     if [[ "$ENABLE_MEMPRESS" -eq 1 ]] ; then
-	git submodule update --init generators/mempress || exit 1
+	git submodule update --init --filter=blob:none generators/mempress || exit 1
     fi
 
     if [[ "$ENABLE_SATURN" -eq 1 ]] ; then
-	git submodule update --init --recursive generators/saturn || exit 1
+	git submodule update --init --filter=blob:none --recursive generators/saturn || exit 1
     fi
 
     if [[ "$ENABLE_SODOR" -eq 1 ]] ; then
-	git submodule update --init generators/riscv-sodor || exit 1
+	git submodule update --init --filter=blob:none generators/riscv-sodor || exit 1
     fi
 
     if [[ "$ENABLE_IBEX" -eq 1 ]] ; then
-	git submodule update --init --recursive generators/ibex || exit 1
+	git submodule update --init --filter=blob:none --recursive generators/ibex || exit 1
     fi
 
     if [[ "$ENABLE_VEXIIRISCV" -eq 1 ]] ; then
-	git submodule update --init generators/vexiiriscv || exit 1
+	git submodule update --init --filter=blob:none generators/vexiiriscv || exit 1
         git -C generators/vexiiriscv submodule update --init VexiiRiscv || exit 1
         git -C generators/vexiiriscv/VexiiRiscv submodule update --init ext/SpinalHDL || exit 1
         git -C generators/vexiiriscv/VexiiRiscv submodule update --init ext/rvls || exit 1
     fi
 
     if [[ "$ENABLE_TACIT" -eq 1 ]] ; then
-	git submodule update --init generators/tacit || exit 1
+	git submodule update --init --filter=blob:none generators/tacit || exit 1
     fi
 
     if [[ "$ENABLE_GEMMINI" -eq 1 ]] ; then
         submodule_name="generators/gemmini"
-        git submodule update --init generators/gemmini || exit 1
+        git submodule update --init --filter=blob:none generators/gemmini || exit 1
         git -C generators/gemmini/ submodule update --init --recursive software/gemmini-rocc-tests || exit 1
     fi
 
     # Non-recursive clone
     submodule_name="generators/rocket-chip"
-    git submodule update --init generators/rocket-chip || exit 1
+    git submodule update --init --filter=blob:none generators/rocket-chip || exit 1
 
     # Minimal non-recursive clone to initialize sbt dependencies
     submodule_name="sims/firesim"
-    git submodule update --init sims/firesim || exit 1
+    git submodule update --init --filter=blob:none sims/firesim || exit 1
     git config --local submodule.sims/firesim.update none
 
     # Non-recursive clone
     submodule_name="tools/rocket-dsp-utils"
-    git submodule update --init tools/rocket-dsp-utils || exit 1
+    git submodule update --init --filter=blob:none tools/rocket-dsp-utils || exit 1
 
     # Non-recursive clone
     submodule_name="tools/dsptools"
-    git submodule update --init tools/dsptools || exit 1
+    git submodule update --init --filter=blob:none tools/dsptools || exit 1
 
     # Only shallow clone needed for basic SW tests
     submodule_name="software/firemarshal"
-    git submodule update --init software/firemarshal || exit 1
+    git submodule update --init --filter=blob:none software/firemarshal || exit 1
 )
 
 # Configure firemarshal to know where our firesim installation is
